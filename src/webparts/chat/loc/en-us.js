define([], function() {
  return {
    "PropertyPaneDescription": "Talks to the real AskStarBulk backend using your own signed-in identity. Pre-filled with the production resource/endpoint — change only if testing against a different backend.",
    "BasicGroupName": "Backend settings",
    "ChatResourceUriFieldLabel": "AAD App ID URI (resource)",
    "ChatQueryUrlFieldLabel": "Query endpoint URL"
  };
});
